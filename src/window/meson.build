include_window = files([
                        'WindowHandle.h',
                        'WindowManager.h'
                      ])

source_window = files([
                       'WindowHandle.cpp'
                     ])

subdir('sdl2')

install_headers(include_window, subdir: 'S3DE/window')
