include_window = files([
  'WindowHandle.h',
  'WindowManager.h'
])

include_window_sdl2 = files([
'sdl2/SDL2GLWindow.h'
])

source_window = files([
  'WindowHandle.cpp'
])

source_window_sdl2 = files([
 'sdl2/SDL2GLWindow.cpp'
])
